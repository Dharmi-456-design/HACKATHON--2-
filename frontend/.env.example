# Backend API base URL. Origin only (no /api suffix) — leave empty to default to http://localhost:5000 in dev
VITE_API_URL=

# Required for Google sign-in via Supabase (https://supabase.com/dashboard -> Project Settings -> API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
